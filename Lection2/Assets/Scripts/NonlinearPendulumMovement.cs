using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//метод Рунге-Кутты 4-го порядка точности

public class NonlinearPendulumMovement : MonoBehaviour
{
    private float g = 9.8f;
    public float Length = 1; //длина нити
    public float startingAngle = 1; //начальное отклонение (градусы)
    public int n = 10; // количество итераций



void RungeKutta(float h, int n)
{

}

// Start is called before the first frame update
void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
